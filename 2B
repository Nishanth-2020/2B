<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Kumaran Nursery — Plants & Gardening Supplies</title>
<style>
  /* Root colors */
  :root {
    --primary: #2e7d32; /* dark green */
    --primary-light: #a5d6a7; /* light green */
    --secondary: #e8f5e9; /* pale green */
    --accent: #66bb6a; /* medium green */
    --text-dark: #1b5e20; /* darker green */
    --text-muted: #4caf50;
    --border-radius: 14px;
    --shadow: 0 6px 18px rgba(46, 125, 50, 0.15);
    --transition: 0.3s ease;
  }

  /* Reset & base */
  *, *::before, *::after {
    box-sizing: border-box;
  }

  body {
    margin: 0; 
    font-family: 'Poppins', sans-serif;
    background: var(--secondary);
    color: var(--text-dark);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Header */
  header {
    background: var(--primary);
    color: var(--secondary);
    padding: 20px 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: var(--shadow);
    border-bottom-left-radius: 24px;
    border-bottom-right-radius: 24px;
  }
  header .logo {
    font-size: 28px;
    font-weight: 800;
    letter-spacing: 2px;
    font-family: 'Courier New', Courier, monospace;
  }
  header .tagline {
    font-weight: 300;
    font-size: 14px;
    opacity: 0.9;
    margin-top: 4px;
  }
  header input[type="search"] {
    border-radius: 18px;
    padding: 8px 16px;
    border: none;
    width: 280px;
    font-size: 15px;
    transition: box-shadow var(--transition);
  }
  header input[type="search"]:focus {
    outline: none;
    box-shadow: 0 0 8px var(--accent);
  }
  header button {
    margin-left: 15px;
    padding: 10px 20px;
    background: var(--accent);
    border: none;
    border-radius: 18px;
    color: var(--secondary);
    font-weight: 600;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  header button:hover {
    background: #4caf50;
  }

  /* Main layout */
  .container {
    max-width: 1100px;
    margin: 25px auto;
    display: flex;
    gap: 30px;
    padding: 0 15px;
  }

  /* Sidebar - form & filters */
  aside.sidebar {
    flex: 0 0 360px;
    background: white;
    border-radius: var(--border-radius);
    padding: 22px 24px;
    box-shadow: var(--shadow);
    height: fit-content;
    position: sticky;
    top: 30px;
  }
  aside.sidebar h2 {
    margin-top: 0;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 12px;
  }
  label {
    display: block;
    margin: 14px 0 6px;
    font-weight: 600;
    font-size: 13px;
    color: var(--text-muted);
  }
  input[type="text"],
  input[type="number"],
  select,
  textarea {
    width: 100%;
    padding: 10px 12px;
    border-radius: 12px;
    border: 1.5px solid #ddd;
    font-size: 14px;
    resize: vertical;
    transition: border-color var(--transition);
  }
  input[type="text"]:focus,
  input[type="number"]:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--accent);
  }
  textarea {
    min-height: 80px;
  }
  button.primary-btn {
    margin-top: 18px;
    width: 100%;
    background: var(--primary);
    color: var(--secondary);
    font-weight: 700;
    border: none;
    padding: 12px 0;
    border-radius: 20px;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  button.primary-btn:hover {
    background: #1b5e20;
  }
  button.clear-btn {
    margin-top: 10px;
    width: 100%;
    background: transparent;
    border: 1.5px solid var(--primary);
    color: var(--primary);
    font-weight: 600;
    border-radius: 20px;
    cursor: pointer;
    transition: background-color var(--transition), color var(--transition);
  }
  button.clear-btn:hover {
    background: var(--primary);
    color: var(--secondary);
  }

  /* Filters */
  .filters {
    margin-top: 28px;
    border-top: 1.5px solid #eee;
    padding-top: 18px;
  }
  .filters h3 {
    margin: 0 0 12px;
    font-weight: 700;
    color: var(--primary);
  }
  .filters select {
    margin-bottom: 12px;
  }

  /* Listings area */
  main.listings-area {
    flex: 1;
  }
  .listings-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 18px;
  }
  .listings-header h2 {
    margin: 0;
    font-weight: 700;
    font-size: 22px;
    color: var(--primary);
  }
  .view-toggle button {
    background: transparent;
    border: none;
    font-weight: 700;
    font-size: 15px;
    padding: 6px 16px;
    border-radius: 18px;
    cursor: pointer;
    color: var(--text-muted);
    transition: background-color var(--transition), color var(--transition);
  }
  .view-toggle button.active {
    background: var(--primary);
    color: var(--secondary);
  }
  .view-toggle button:not(.active):hover {
    background: var(--primary-light);
    color: var(--primary);
  }

  /* Grid layout */
  .grid-listings {
    display: grid;
    grid-template-columns: repeat(auto-fill,minmax(240px,1fr));
    gap: 20px;
  }
  .card {
    background: white;
    border-radius: var(--border-radius);
    box-shadow: var(--shadow);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease;
  }
  .card:hover {
    transform: translateY(-6px);
  }
  .card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-bottom: 2px solid var(--accent);
  }
  .card-content {
    padding: 14px 18px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }
  .card-content h3 {
    margin: 0 0 6px;
    font-weight: 700;
    font-size: 18px;
    color: var(--primary);
  }
  .card-content p.description {
    flex-grow: 1;
    font-size: 14px;
    color: var(--text-muted);
    margin: 0 0 10px;
  }
  .card-content .price {
    font-weight: 800;
    font-size: 17px;
    color: var(--accent);
    margin-bottom: 10px;
  }
  .card-content .category {
    font-size: 13px;
    font-weight: 600;
    color: var(--primary-light);
    margin-bottom: 10px;
  }
  .card-content .contact-btn {
    padding: 10px 0;
    border-radius: 20px;
    border: none;
    background: var(--primary);
    color: var(--secondary);
    font-weight: 600;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  .card-content .contact-btn:hover {
    background: #1b5e20;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .container {
      flex-direction: column;
      padding: 0 10px;
    }
    aside.sidebar {
      position: static;
      width: 100%;
      margin-bottom: 30px;
    }
  }
</style>
</head>
<body>

<header>
  <div class="logo">Kumaran Nursery</div>
  <div class="tagline">Plants & Gardening Supplies</div>
  <input type="search" placeholder="Search plants & items..." />
  <button>Search</button>
</header>

<div class="container">
  <aside class="sidebar">
    <h2>Filter Items</h2>
    <label for="category">Category</label>
    <select id="category">
      <option value="">All Categories</option>
      <option value="indoor">Indoor Plants</option>
      <option value="outdoor">Outdoor Plants</option>
      <option value="tools">Gardening Tools</option>
      <option value="seeds">Seeds</option>
      <option value="soil">Soil & Fertilizers</option>
      <option value="pots">Pots & Planters</option>
    </select>

    <label for="price-min">Min Price</label>
    <input type="number" id="price-min" placeholder="0" />

    <label for="price-max">Max Price</label>
    <input type="number" id="price-max" placeholder="1000" />

    <button class="primary-btn">Apply Filters</button>
    <button class="clear-btn">Clear Filters</button>
  </aside>

  <main class="listings-area">
    <div class="listings-header">
      <h2>Our Products</h2>
      <div class="view-toggle">
        <button class="active">Grid View</button>
        <button>List View</button>
      </div>
    </div>

    <div class="grid-listings">
      <!-- Product cards -->
      <div class="card">
        <img src="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?auto=format&fit=crop&w=400&q=80" alt="Snake Plant" />
        <div class="card-content">
          <h3>Snake Plant</h3>
          <p class="description">Easy-care indoor plant that purifies the air.</p>
          <div class="price">₹350</div>
          <div class="category">Indoor Plants</div>
          <button class="contact-btn">Contact Us</button>
        </div>
      </div>

      <div class="card">
        <img src="https://images.unsplash.com/photo-1464965911861-746a04b4bca7?auto=format&fit=crop&w=400&q=80" alt="Rose Plant" />
        <div class="card-content">
          <h3>Rose Plant</h3>
          <p class="description">Beautiful flowering plant that brightens any garden.</p>
          <div class="price">₹200</div>
          <div class="category">Outdoor Plants</div>
          <button class="contact-btn">Contact Us</button>
        </div>
      </div>

      <div class="card">
        <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=400&q=80" alt="Garden Shovel" />
        <div class="card-content">
          <h3>Garden Shovel</h3>
          <p class="description">Durable shovel perfect for digging and planting.</p>
          <div class="price">₹450</div>
          <div class="category">Gardening Tools</div>
          <button class="contact-btn">Contact Us</button>
        </div>
      </div>

      <div class="card">
        <img src="https://images.unsplash.com/photo-1504198453319-5ce911bafcde?auto=format&fit=crop&w=400&q=80" alt="Tomato Seeds" />
        <div class="card-content">
          <h3>Tomato Seeds</h3>
          <p class="description">High-yield seeds for fresh homegrown tomatoes.</p>
          <div class="price">₹80</div>
          <div class="category">Seeds</div>
          <button class="contact-btn">Contact Us</button>
        </div>
      </div>

      <div

